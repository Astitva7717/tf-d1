apiVersion: v1
kind: Service
metadata:
  name: {{ include "ram-backend.fullname" . }}
  labels:
    {{- include "ram-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ram-backend.selectorLabels" . | nindent 4 }}
