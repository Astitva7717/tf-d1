apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ram-backend.fullname" . }}-test-connection"
  labels:
    {{- include "ram-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ram-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
