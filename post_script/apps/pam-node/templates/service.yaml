apiVersion: v1
kind: Service
metadata:
  name: {{ include "pam-node.fullname" . }}
  labels:
    {{- include "pam-node.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pam-node.selectorLabels" . | nindent 4 }}
