apiVersion: v1
kind: Service
metadata:
  name: {{ include "pam-frontend.fullname" . }}
  labels:
    {{- include "pam-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pam-frontend.selectorLabels" . | nindent 4 }}
