apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pam-frontend.fullname" . }}-test-connection"
  labels:
    {{- include "pam-frontend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pam-frontend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
