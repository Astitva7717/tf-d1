apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "comm-node.fullname" . }}-test-connection"
  labels:
    {{- include "comm-node.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "comm-node.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
