apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "comm-frontend.fullname" . }}-test-connection"
  labels:
    {{- include "comm-frontend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "comm-frontend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
