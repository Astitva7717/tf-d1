---
apiVersion: v1
kind: Service
metadata:
  name: rg-front
  namespace: sapidblue
spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP
  selector:
    app: rg-front