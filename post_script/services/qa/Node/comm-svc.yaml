---
apiVersion: v1
kind: Service
metadata:
  name: comm-node
  namespace: sapidblue
spec:
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
  selector:
    app: comm-node